#!/usr/local/bin/jondocker

image jshridha/rdiffweb

#port 80:8080

map $HOME/Backup to /backups
map $HOME/Restore to /restores
map $HOME/rdiffweb_files/conf to /etc/rdiffweb

network extern

params -h backup.anonymous-proxy-servers.net -e VIRTUAL_PORT=8080
